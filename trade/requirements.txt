python-telegram-bot==21.6
python-dotenv==1.0.1
# 산업트렌드 🔍추가신호(Gemini 2.5 Pro)용 — 선택적.
# 미설치 또는 GOOGLE_API_KEY 미설정 시 박스만 조용히 생략(파이프라인 정상).
langchain-google-genai>=2.0
# Vertex AI backend (optional) — only used when GOOGLE_GENAI_USE_VERTEXAI=true.
# Routes the trade bot's Gemini calls through Vertex (ADC, no API key).
langchain-google-vertexai>=2.0
# JPX 상장 목록 빌더(build_jpx_codes)가 쓴다 — 설치본엔 이미 있다(2026-09-23
# jp_master_probe ② 실측 2.34.2). 전이 의존에 기대지 않게 명시한다.
requests>=2.31
