[pytest]
testpaths = bot/tests TradingAgents/tests
addopts = -ra --strict-markers
markers =
    unit: fast isolated unit tests
    integration: tests requiring external services
    smoke: quick sanity-check tests
