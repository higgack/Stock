---
priority: 1
targets:
  - "**"
---

# NOAH 주식 분석 봇 — 에이전트 설정

## 응답 방식

모든 답변은 **한국어**로 작성하며, **비개발자 친화적**으로 설명합니다.

## 운영 반영 규칙 (고정)

운영 대시보드/운영 봇에 실제 반영이 목적이면, 다음 순서를 **항상 필수**로 따릅니다.

1. 코드 수정 및 저장
2. 테스트/검증
3. 커밋
4. 푸시
5. 배포 브랜치(base) 반영 확인

즉, 로컬 확인 단계에서는 커밋 없이 진행할 수 있지만, 운영 반영 단계에서는
"수정만 하고 미커밋" 상태로 완료 처리하지 않습니다.

또한 매 작업 종료 시 아래 3가지를 기본 안내합니다.
- 지금 커밋 가능한지
- 커밋 제외 파일(로그/임시파일)
- 권장 커밋 메시지

### 기본 원칙

1. **기술 용어 한글화**: API → 데이터 요청 인터페이스, Cache → 빠른 재사용을 위한 임시 저장
2. **비즈니스 관점 우선**: 코드 구조보다 사업 영향을 먼저 설명
3. **그림으로 표현**: 텍스트 다이어그램으로 아키텍처 시각화
4. **괄호 부연**: 개발자 용어 사용 시 괄호로 설명 (예: 배포(코드를 서버에 올리기))

## 우선 전략 (2026-07-29~)

### 1순위: **비용 최적화 + 속도 개선**

#### 비용 문제
- 현재: Google Gemini Pro 모든 단계에서 사용 → 비용 높음
- 목표: 월 AI 비용 30% 절감
- 방법:
  - ✅ 분석 단계는 저가 모델(Gemini Flash) 사용
  - ✅ 최종 결정 단계만 고가 모델(Pro) 사용
  - ✅ 같은 종목 반복 분석 시 캐싱으로 재사용

#### 속도 문제
- 현재: 1-3분 소요
- 목표: 30초 이내
- 방법:
  - ✅ 4명 분석가를 순차→병렬 처리 (동시 실행)
  - ✅ 데이터 캐싱 적극화 (10시간 유지)
  - ✅ 불필요한 데이터 수집 제거

### 2순위: **팀 사용 기능 추가** (비용 최적화 후)
- 다중 사용자 로그인
- 팀 포트폴리오 공유
- 분석 이력 협업

### 3순위: **API 공개** (팀 기능 안정 후)
- REST API로 외부 앱 연동 가능
- 배치 분석 (100개 종목 한번에)

## 의사결정 기준

변경 제안 시 다음 순서로 판단:

```
이것이 "비용↓ 또는 속도↑"에 도움되는가?
  ├─ YES → 우선 검토
  ├─ MAYBE → 다른 영향 확인 (사용성, 안정성)
  └─ NO → 2순위 이후 검토
```

## 설명 예시

### ❌ 하지 말 것
> "LLM 모델 라우팅 레이어를 Adapter 패턴으로 리팩토링하고, 토큰 최적화를 위해 prompt engineering을 적용하면 됩니다."

### ✅ 이렇게 설명
> **문제**: 분석 비용이 높고 결과도 느립니다.
> 
> **원인**: 모든 의사결정을 비싼 AI(Gemini Pro)에 물어보기 때문입니다.
>
> **해결책**:
> 1. 보조 작업(데이터 수집, 뉴스 읽기)은 저가 AI(Gemini Flash)에 맡기기 → 비용 50% 절감
> 2. 최종 투자 결정만 비싼 AI에 물어보기
> 3. 한번 분석한 종목을 다시 분석할 때는 저장된 결과 재사용 → 속도 100배 향상
>
> **예상 효과**: 비용 30% ↓, 속도 3배 ↑

## 이 파일의 역할

이 파일은 모든 대화에서 자동으로 적용됩니다. 변경이 필요하면:
- "에이전트 설정 수정"이라고 말씀하면 AGENT.md 업데이트됩니다.
- 개별 대화에서 규칙을 다시 설정할 필요 없습니다.
